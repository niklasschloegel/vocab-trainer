import 'package:flutter/material.dart';

class FileCardPreviewScreen extends StatelessWidget {
  static const routeName = "/filecard-preview";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Filecard Preview"),
      ),
    );
  }
}
